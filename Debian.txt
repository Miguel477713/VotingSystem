#Enable on restart at multi-user.target
sudo systemctl daemon-reload
sudo systemctl enable --now voting-tcp.service voting-failover.service

#Process status
sudo systemctl status voting-gateway.service
sudo journalctl -u voting-gateway.service -f
journalctl -u voting-failover.service -n 50 --no-pager

#Stop services
sudo systemctl stop voting-gateway.service voting-tcp.service voting-failover.service

#Start services
sudo systemctl start voting-gateway.service voting-tcp.service

#Manual assignment of IP to ethernet interface
sudo ip addr add 169.254.236.20/24 dev eth0  
sudo ip link set eth0 up

#Reset IP interface
sudo ip addr flush dev eth0

#List listening TCP/UDP ports
ss -tulnp

#Set env variable for current session
export VOTING_SQL_CONNECTION_STRING="Server=tcp:voting-system-server.database.windows.net,1433;Initial Catalog=VotingSystemDatabase;Persist Security Info=False;User ID=miguel;Password=squareTemp1;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;"

#Set env variable permanently
echo 'export VOTING_SQL_CONNECTION_STRING="Server=tcp:voting-system-server.database.windows.net,1433;Initial Catalog=VotingSystemDatabase;Persist Security Info=False;User ID=miguel;Password=squareTemp1;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;"' >> ~/.bashrc
source ~/.bashrc
