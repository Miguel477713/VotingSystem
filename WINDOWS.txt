Powershell commands

#Remove permissions so SSH does not fail
    # Remove inherited permissions
    icacls .\squareroot.pem /inheritance:r

    # Remove common broad principals (may or may not exist; errors are ok)
    icacls .\squareroot.pem /remove "NT AUTHORITY\Authenticated Users" "BUILTIN\Users" "Everyone" 2>$null

    # Grant read permission ONLY to your user
    icacls .\squareroot.pem /grant:r "$($env:USERNAME):(R)"

#Create ssh agent to  securely store private SSH KEY securely stores your private SSH
Set-Service -Name ssh-agent -StartupType Manual
Start-Service ssh-agent
Get-Service ssh-agent
ssh-add squareroot.pem
scp Server.py HTTPGateway.py azureuser@20.120.242.3:~/voting/

#List listening TCP/UDP ports
netstat -ano | findstr LISTENING

#Remove firewall restrictions to allow communication over ethernet
Set-NetFirewallProfile -Profile Domain,Public,Private -Enabled False

#Set env variable for current session
set VOTING_SQL_CONNECTION_STRING=Server=tcp:voting-system-server.database.windows.net,1433;Initial Catalog=VotingSystemDatabase;Persist Security Info=False;User ID=miguel;Password=squareTemp1;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;

#Set env variable permanently
setx VOTING_SQL_CONNECTION_STRING "Driver={ODBC Driver 18 for SQL Server};Server=tcp:voting-system-server.database.windows.net,1433;Database=VotingSystemDatabase;Uid=miguel;Pwd=squareTemp1;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;"

